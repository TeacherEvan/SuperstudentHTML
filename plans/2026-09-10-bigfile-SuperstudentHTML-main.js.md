# Refactor plan — `src/js/core/main.js` in `TeacherEvan/SuperstudentHTML`

> Auto-generated by `cron_refactor_planner.py` on 2026-09-10T23:52:49.775852+07:00.
> v2 — file-aware: objectives are derived from structural analysis (exports, hooks,
> render branches, repeated literals, import cross-refs), NOT a generic N-slice filler.

## File header

| field | value |
|---|---|
| repository | `TeacherEvan/SuperstudentHTML` |
| path | `src/js/core/main.js` |
| size | 21.6 KB (22,108 bytes) |
| lines | 700 |
| generated | 2026-09-10T23:52:49.775868+07:00 |

### Top imports (sample)
  - `import { GAME_CONFIG } from '../config/constants.js';`
  - `import CenterPieceManager from '../game/managers/centerPieceManager.js';`
  - `import CheckpointManager from '../game/managers/checkpointManager.js';`
  - `import FlamethrowerManager from '../game/managers/flamethrowerManager.js';`
  - `import GlassShatterManager from '../game/managers/glassShatterManager.js';`
  - `import HudManager from '../game/managers/hudManager.js';`
  - `import MultiTouchManager from '../game/managers/multiTouchManager.js';`
  - `import { ProgressManager } from '../game/managers/progressManager.js';`
  - `import { LevelMenu } from '../ui/components/levelMenu.js';`
  - `import { LevelCompletionScreen } from '../ui/components/levelCompletionScreen.js';`

## Structural analysis

**Repeated literals (≥2×):**
- `PerformanceLevelChanged` (×2)

**Top-referenced imports:**
- `eventTracker` (18 refs)
- `GAME_CONFIG` (5 refs)
- `ProgressManager` (2 refs)
- `LevelMenu` (2 refs)
- `LevelCompletionScreen` (2 refs)
- `loadLevelModule` (2 refs)

## Why this file matters

This is the largest source file under the standard source roots
(`src, app, lib, convex, components, pages, src-tauri/src`) in `SuperstudentHTML`. The objectives below are
derived from the structural signals above — they name concrete extractions
(line-anchored, symbol-anchored) rather than generic slice-N placeholders.

## Objectives (12 tickable)

### OBJ-001 — Hoist repeated literal `'PerformanceLevelChanged'` (×2) to a constant
- **Target:** String literal appears 2× in `src/js/core/main.js`. Extract to
  `./constants.ts` (already exists per imports) or co-located `strings.ts`.
- **Why:** Repeated literals indicate copy that should be localizable or reused.
- **Acceptance:** zero occurrences of the literal in `src/js/core/main.js` after the change;
  constant referenced via the barrel.
- **Validation:** `rg -F '<literal>' src/js/core/main.js` returns 0.
- **Evidence:** grep before/after.

### OBJ-002 — Confirm `eventTracker` (18 refs) is the right shared utility
- **Target:** `eventTracker` is the most-referenced import in this file (18 uses).
- **Action:** verify whether other dashboard pages also import it; if so, ensure it lives
  in a shared `lib/` or `@/components/ui/` and not in a feature-local path.
- **Acceptance:** document the canonical home for `eventTracker` in `docs/`.
- **Validation:** `rg -l 'eventTracker' src/` — should match ≤3 call sites in
  feature code, more in shared.
- **Evidence:** call-site map.

### OBJ-003 — Run knip / ts-prune against the page after extraction
- **Target:** After OBJ-001 → OBJ-006 land, some imports in `src/js/core/main.js` will be unused.
- **Tool:** `pnpm dlx knip --reporter compact` or `pnpm dlx ts-prune`.
- **Acceptance:** zero unused exports; zero dead imports in `src/js/core/main.js`.
- **Validation:** `rg -n '^import' src/js/core/main.js` cross-checked against actually-used symbols.
- **Evidence:** knip output diff before/after.

### OBJ-004 — Audit sibling `index.ts` barrels for circular / stale re-exports
- **Target:** `src/js/core/components/index.ts` (or its peer in
  any newly split directory).
- **Acceptance:** barrel exports only symbols whose source file exists.
- **Validation:** `rg -n 'export' barrel.ts | wc -l` matches live exports.
- **Evidence:** barrel diff.

### OBJ-005 — Reduce `src/js/core/main.js` below 350 lines via cumulative extractions
- **Target:** After OBJ-001 → OBJ-008 land, the remaining render block should
  be a thin composition of named sub-components.
- **Goal:** `src/js/core/main.js` shrinks from 700 to ≤350 lines.
- **Acceptance:** `wc -l` shows the reduction; behavior unchanged via tests + e2e.
- **Validation:** `pnpm run build && pnpm run test && pnpm run test:e2e` (full gate).
- **Evidence:** before/after line count + green build.

### OBJ-006 — Hardening pass 6
- **Target:** Tighten types / tighten prop drilling in `src/js/core/main.js` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-007 — Hardening pass 7
- **Target:** Tighten types / tighten prop drilling in `src/js/core/main.js` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-008 — Hardening pass 8
- **Target:** Tighten types / tighten prop drilling in `src/js/core/main.js` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-009 — Hardening pass 9
- **Target:** Tighten types / tighten prop drilling in `src/js/core/main.js` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-010 — Hardening pass 10
- **Target:** Tighten types / tighten prop drilling in `src/js/core/main.js` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-011 — Hardening pass 11
- **Target:** Tighten types / tighten prop drilling in `src/js/core/main.js` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-012 — Hardening pass 12
- **Target:** Tighten types / tighten prop drilling in `src/js/core/main.js` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

## Definition of Done

- [ ] Every OBJ-00X above has a Definition-of-Done entry below it filled in by the IMPLEMENT pass.
- [ ] The original `src/js/core/main.js` either disappears (split into smaller modules) or shrinks materially while preserving behavior.
- [ ] `pnpm run type-check && pnpm run lint && pnpm run test && pnpm run build` is green (Next.js full gate).
- [ ] `git diff --stat` shows only the planned paths.
- [ ] `debrief.md` (per surgical-implementation) records evidence per objective.

## Security notes

- This plan modifies source under `SuperstudentHTML`. No credential rotation, no
  permission changes, no production deploy. APPROVAL_GATE = REVIEW.
- `git push` will target the same branch the cronjob cloned from; if push is
  rejected the plan remains in the working tree and the next tick retries.

## Provenance

- planner: `cron_refactor_planner.py` (v2 — file-aware objectives)
- plan filename: `2026-09-10-bigfile-SuperstudentHTML-main.js.md`
- reviewer: `cron_plan_reviewer.py` will append a `## REVIEW <iso>` section on
  its next tick.
- implementer: `cron_surgical_impl.py` will pick this plan up once the reviewer
  marks it `READY` or `READY-WITH-WARNINGS`.


## REVIEW 2026-09-11T00:04:07.252844+07:00

**Verdict:** `NEEDS-REVISION`

**Structural check:** objectives=12 file_header=✓ imports=✓ why=✓ dod=✓ security=✓

**Gaps:**
1. **OBJ-006–OBJ-011 are identical boilerplate filler** — six "Hardening pass N" entries with verbatim duplicate text, contradicting the plan's own claim that objectives are "derived from structural analysis … NOT a generic N-slice filler." OBJ-012 is truncated/incomplete. These must be replaced with distinct, signal-derived objectives or removed.
2. **Missing DOD and security sections** — structural check reports `has_dod=None`, `has_security=None`. No Definition of Done, no security/privacy considerations for a file handling game state, user progress, and event tracking.
3. **`.js`/`.ts` extension inconsistency** — plan references `./constants.ts`, `src/js/core/components/index.ts` but the actual file is `main.js` and imports use `.js` extensions. The barrel path `src/js/core/components/index.ts` may not exist; this must be verified before OBJ-004 can proceed.
